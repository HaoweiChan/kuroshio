__pycache__/
*.pyc
*.egg-info/
.venv/
dist/
.pytest_cache/
.DS_Store
# runtime data (repo root only) — never commit user data or cached reports
/data/
/facets/
*.env

# kuroshio research output (paid or session mode) lands here when run from the checkout root
reports/
